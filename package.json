{
  "name": "embeddocxeditor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Offline-first, full-fidelity DOCX editor in the browser, built on LibreOffice WebAssembly (ZetaJS).",
  "license": "MIT",
  "scripts": {
    "copy:vendor": "node scripts/copy-vendor.mjs",
    "fetch:wasm": "node scripts/fetch-wasm.mjs",
    "predev": "npm run copy:vendor && npm run fetch:wasm",
    "dev": "vite",
    "prebuild": "npm run copy:vendor && npm run fetch:wasm",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test src/embed-trust.test.ts src/engine/pending.test.ts src/engine/url-safe.test.ts"
  },
  "dependencies": {
    "zetajs": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
